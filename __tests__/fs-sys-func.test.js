const { makeItDIRPdrop } = require('../lib/file-sys-funct');

describe ('fs function that does stuff') = () => {
    describe (makeItDIRPdrop), () => {
        it('shoudl return a made directory and all parent directories'), () => {
            expect(makeItDIRPdrop('./thebigpath/thelittlepath') => {
r
            }
        }